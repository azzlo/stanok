<div class="container">
  <%= form_for(client) do |f| %>
    <% if client.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

        <ul>
        <% client.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row">
      <div class="field input-field col s12 m6">
        <i class="material-icons prefix">mode_edit</i>
        <%= f.label :Nombre, for: :client_name %>
        <%= f.text_field :name %>
      </div>

      <div class="field input-field col s12 m6">
        <i class="material-icons prefix">account_circle</i>
        <%= f.label :Nombre_de_contacto, for: :client_contact_name %>
        <%= f.text_field :contact_name %>
      </div>
    </div>

    <div class="row">
      <div class="field input-field col s12 m6">
        <i class="material-icons prefix">call</i>
        <%= f.label :Telefono, for: :client_phone %>
        <%= f.text_field :phone %>
      </div>

      <div class="field input-field col s12 m6 l6">
        <i class="material-icons prefix">email</i>
        <%= f.label :Correo_electronico, for: :client_email %>
        <%= f.text_field :email %>
      </div>
    </div>
    <div class="row">
      <div class="field input-field col s12">
        <i class="material-icons prefix">location_on</i>
        <%= f.label :address, 'Dirección' %>
        <%= f.text_field :address %>
      </div>
      <div class="input-field col s6">
        <i class="material-icons prefix">center_focus_strong</i>
        <%= f.label :latitude, 'Latitud' %>
        <%= f.text_field :latitude %>
      </div>
      <div class="input-field col s6">
        <i class="material-icons prefix">center_focus_strong</i>
        <%= f.label :longitude, 'Longitud' %>
        <%= f.text_field :longitude %>
      </div>
    </div>

    <div class="row">
      <div class="col s12 right-align actions ">
        <%= f.submit class: "btn-large waves-effect" %>
      </div>
    </div>
  <% end %>

</div>
