<%= form_for([@service, @service_charge]) do |f| %>
  <% if service_charge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_charge.errors.count, "error") %> prohibited this service_charge from being saved:</h2>

      <ul>
      <% service_charge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col s12 m3 input-field">
      <i class="material-icons prefix">attach_money</i>
      <%= f.label :amount, for: :service_charge_amount %>
      <%= f.text_field :amount %>
    </div>

    <div class="col s12 m9 input-field">
      <%= f.label :description, for: :service_charge_description   %>
      <%= f.text_field :description %>
    </div>

    <div class="row">
      <div class="col s12 right-align actions">
        <%= f.submit class: "btn" %>
      </div>
    </div>

  </div>
<% end %>
