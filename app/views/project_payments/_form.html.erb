<div class="container">

  <%= form_for [@project, @project_payment] do |f| %>
    <% if project_project_payment.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(project_project_payment.errors.count, "error") %> prohibited this project_payment from being saved:</h2>

        <ul>
        <% project_project_payment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field input-field col s6">
      <%= f.label :project_id %>
      <%= f.text_field :project_id %>
    </div>

    <div class="field input-field col s6">
      <%= f.label :payment %>
      <%= f.text_field :payment %>
    </div>

    <div class="field input-field col s6">
      <%= f.label :description %>
      <%= f.text_field :description %>
    </div>

    <div class="actions waves-effect waves-light btn">
      <%= f.submit %>
    </div>
  <% end %>

</div>
